const sources = {
  url: { label: 'WHATWG URL Standard', url: 'https://url.spec.whatwg.org/', note: 'URL 구성 요소와 파싱 모델을 확인합니다.' },
  http: { label: 'RFC 9110 · HTTP Semantics', url: 'https://www.rfc-editor.org/rfc/rfc9110', note: 'HTTP method, status, header의 의미론입니다.' },
  messaging: { label: 'RFC 9112 · HTTP/1.1', url: 'https://www.rfc-editor.org/rfc/rfc9112', note: 'HTTP 메시지 형식과 연결 관리를 확인합니다.' },
  cookies: { label: 'RFC 6265 · HTTP State Management Mechanism', url: 'https://www.rfc-editor.org/rfc/rfc6265', note: 'Cookie 상태 관리의 기본 규격입니다.' },
  fetch: { label: 'WHATWG Fetch Standard', url: 'https://fetch.spec.whatwg.org/', note: 'Origin, CORS, Fetch 처리 모델을 확인합니다.' },
  html: { label: 'WHATWG HTML Living Standard', url: 'https://html.spec.whatwg.org/', note: '브라우저 문서·DOM·script 실행 모델을 확인합니다.' },
  mdn: { label: 'MDN Web Docs · HTTP and Browser APIs', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP', note: '브라우저 관찰과 실무 예시를 보완하는 공식 문서입니다.' },
}

const blockSet = ({ id, question, explanation, transcript, comparison, caseBody, facts, misconception, checks, work, labs, sourceItems, summary, warning, diagram, timeline }) => [
  { type: 'question', title: '이 모듈이 답할 질문', body: question },
  { type: 'explanation', title: '핵심 원리', paragraphs: explanation },
  ...(diagram ? [{ type: 'diagram', title: diagram.title, body: diagram.body, nodes: diagram.nodes }] : []),
  ...(timeline ? [{ type: 'timeline', title: timeline.title, items: timeline.items }] : []),
  { type: transcript.type || 'terminal', sourceType: 'educational-reconstruction', title: transcript.title, command: transcript.command, message: transcript.message, code: transcript.code, output: transcript.output, language: transcript.language, annotations: transcript.annotations },
  { type: 'comparison', title: comparison.title, columns: comparison.columns, rows: comparison.rows },
  { type: 'case', title: 'CodeCureLAB 사례', body: caseBody, facts },
  { type: 'misconception', title: '자주 하는 혼동', items: misconception },
  { type: 'checkpoint', id: `${id}-check-01`, title: checks[0].title, prompt: checks[0].prompt, options: checks[0].options, answer: checks[0].answer, explanation: checks[0].explanation },
  { type: 'checkpoint', id: `${id}-check-02`, title: checks[1].title, prompt: checks[1].prompt, mode: 'short-answer', placeholder: checks[1].placeholder },
  ...(warning ? [{ type: 'warning', title: warning.title, body: warning.body }] : []),
  { type: 'work-context', title: '보안 업무에서의 사용', body: work },
  { type: 'practice-link', title: '다음 활동', body: '고정된 training 사례에서 관찰과 해석을 나누어 기록합니다.', labIds: labs },
  { type: 'sources', title: '공식 참고자료', items: sourceItems },
  { type: 'summary', title: '핵심 정리', bullets: summary },
]

export const week3LessonBlocks = Object.freeze({
  'w3-flow': blockSet({
    id: 'w3-flow', question: '주소창에 URL을 입력한 뒤 CodeCureLAB 화면이 갱신되기까지 어떤 네트워크·서버·브라우저 단계를 거칠까?',
    explanation: ['브라우저는 URL을 파싱하고 도메인이 있으면 DNS로 IP 주소를 확인합니다. HTTPS라면 TCP 연결 뒤 TLS 핸드셰이크를 수행하고, 그 위에서 HTTP 요청을 보냅니다. 요청은 Reverse Proxy, Web Server, Application Server, 데이터 저장소나 외부 서비스로 이어질 수 있습니다.', '응답을 받은 브라우저는 HTML을 파싱해 DOM을 만들고 CSS와 JavaScript를 적용합니다. CDN과 캐시는 응답 경로와 재사용에 관여하지만 애플리케이션의 인증·인가 검증을 대신하지 않습니다. 각 단계는 다른 로그와 도구에서 관찰됩니다.'],
    diagram: { title: 'URL에서 화면까지', nodes: ['URL 파싱', 'DNS 이름 해석', 'TCP·TLS 연결', 'HTTP 요청', 'Proxy·App·Data 처리', 'HTTP 응답', 'HTML·CSS·JS 처리', 'DOM·화면 갱신'] },
    transcript: { type: 'http-message', title: '한 요청의 관찰 지점', message: 'Browser URL: https://training.local/notices\nDNS: training.local → 127.0.0.1\nRequest: GET /notices HTTP/1.1\nResponse: HTTP/1.1 200 OK\nBrowser: HTML parse → DOM update', annotations: ['DNS 결과는 도메인과 IP의 연결 관찰입니다.', 'HTTP 메시지는 TLS 연결 이후의 애플리케이션 요청·응답입니다.', '실행 후 DOM은 서버가 보낸 원문 HTML과 다를 수 있습니다.'] },
    comparison: { title: '단계별로 보는 도구와 흔적', columns: ['단계', '주요 관찰', '대표 도구·기록'], rows: [['DNS', '도메인과 IP 응답', 'DNS 로그·dig(허가 범위)'], ['HTTP', 'method·headers·body·status', 'curl·DevTools·Burp'], ['서버 처리', '앱 로그·권한 판단·오류', '접근·오류 로그'], ['브라우저', 'DOM·JS·저장소', 'DevTools Elements·Application']] },
    caseBody: '학생이 `/notices`를 열면 브라우저가 요청을 만들고, CodeCureLAB 앱이 로그인 상태와 공지 권한을 확인해 응답을 만듭니다. 브라우저는 응답 HTML과 JavaScript를 처리해 화면을 갱신합니다. 같은 사건을 HTTP 원문·서버 로그·DOM에서 각각 다르게 볼 수 있습니다.', facts: ['URL·DNS·TLS·HTTP·DOM은 같은 일이 아니라 연결된 단계다.', '정상 200 응답도 인가·데이터 최소화의 증명은 아니다.', '각 관찰 지점의 시간과 범위를 함께 기록한다.'],
    misconception: ['DNS가 끝나면 웹 페이지가 바로 열린다 → 연결·TLS·HTTP·브라우저 렌더링이 이어진다.', '응답 HTML과 화면 DOM은 항상 같다 → JavaScript와 브라우저 파싱이 실행 후 DOM을 바꿀 수 있다.'],
    checks: [{ title: '순서', prompt: 'HTTPS 요청에서 HTTP 메시지 전송보다 먼저 필요한 단계는?', options: ['TCP 연결과 TLS 핸드셰이크', 'DOM 클릭', 'Git commit'], answer: 0, explanation: 'HTTPS는 보통 TCP 연결 뒤 TLS로 보안 채널을 준비하고 그 위에서 HTTP를 전송합니다.' }, { title: '관찰 계획', prompt: '공지 화면이 예상과 다를 때 URL·HTTP·서버 로그·DOM에서 각각 무엇을 확인할지 적어 보세요.' }],
    work: '웹 보안 진단, 장애 분석, AppSec은 한 화면 현상을 URL·HTTP·서버·DOM의 여러 층으로 나눠 봅니다. 어느 층의 사실인지 구분해야 원인과 책임 범위를 좁힐 수 있습니다.', labs: ['w3-timeline', 'w3-tool-triangle'], sourceItems: [sources.url, sources.http, sources.html], summary: ['URL에서 화면까지는 DNS·연결·HTTP·서버·브라우저 렌더링의 연결이다.', '서버 응답 원문과 실행 후 DOM은 다를 수 있다.', '각 단계의 도구와 로그는 관찰 범위가 다르다.'],
  }),
  'w3-url-dns': blockSet({
    id: 'w3-url-dns', question: 'URL의 scheme, host, port, path, query, fragment는 서버와 브라우저에서 각각 어떻게 다르게 사용될까?',
    explanation: ['URL은 scheme, authority(호스트와 선택적 포트), path, query, fragment 같은 구성 요소를 가집니다. `https://training.local:443/notices?q=exam#latest`에서 fragment는 일반적으로 HTTP 요청에 포함되지 않고 브라우저 안에서 문서 위치나 상태에 사용됩니다.', 'DNS는 도메인 이름을 IP 주소 등 연결 정보로 해석하는 과정입니다. DNS 응답은 캐시·레코드 종류·네트워크에 따라 달라질 수 있습니다. URL 값이 브라우저에 보인다는 사실은 해당 대상에 요청할 허가를 뜻하지 않습니다.'],
    transcript: { type: 'http-message', title: 'URL과 HTTP 요청선의 차이', message: 'Browser URL: https://training.local/notices?q=exam#latest\n\nGET /notices?q=exam HTTP/1.1\nHost: training.local', annotations: ['scheme과 host는 연결·Host header 구성에 사용됩니다.', 'path와 query는 request target에 보입니다.', '`#latest` fragment는 이 HTTP 요청선에 포함되지 않습니다.'] },
    comparison: { title: 'URL 구성 요소의 위치', columns: ['구성 요소', '예시', '주요 처리 위치'], rows: [['scheme', '`https`', '브라우저 연결 정책·프로토콜'], ['host·port', '`training.local:443`', 'DNS·연결 대상·Host'], ['path', '`/notices`', '서버 라우팅'], ['query', '`?q=exam`', '서버·클라이언트 입력 처리'], ['fragment', '`#latest`', '보통 브라우저 내부 상태, HTTP 요청에 미포함']] },
    caseBody: 'CodeCureLAB 검색 URL의 `q`는 서버가 검색 입력으로 처리할 수 있고, `#tab=recent` 같은 fragment는 JavaScript가 화면 상태로 읽을 수 있습니다. 입력이 어느 곳에 도달하는지에 따라 Week 3의 Source·Transform·Sink 추적이 달라집니다.', facts: ['같은 URL 문자열도 구성 요소별 처리 주체가 다르다.', 'fragment는 서버 access log에 보이지 않을 수 있다.', 'URL 입력은 서버와 브라우저 양쪽에서 문맥에 맞게 검증·인코딩해야 한다.'],
    misconception: ['fragment는 서버가 항상 읽는다 → 일반 HTTP 요청에는 포함되지 않으므로 서버 원문과 브라우저 JS를 구분해 본다.', 'DNS IP를 알면 대상 테스트가 허가된다 → 주소 정보와 테스트 권한은 별개다.'],
    checks: [{ title: '요청 포함 여부', prompt: '일반적인 HTTP 요청선에 포함되지 않는 URL 구성 요소는?', options: ['fragment', 'path', 'query'], answer: 0, explanation: 'fragment는 보통 브라우저가 문서 내부 상태를 처리할 때 사용하며 HTTP 요청에 보내지지 않습니다.' }, { title: '입력 흐름', prompt: '검색 query와 fragment가 각각 서버·브라우저 어디에서 처리될 수 있는지와 확인 도구를 적어 보세요.' }],
    work: '웹 개발과 AppSec은 URL 구성 요소를 같은 문자열로 뭉뚱그리지 않습니다. 라우팅·로그·캐시 키·입력 검증·DOM Source가 달라질 수 있기 때문입니다.', labs: ['w3-timeline', 'w3-source-sink'], sourceItems: [sources.url, sources.fetch], summary: ['URL 구성 요소는 서버와 브라우저에서 처리 위치가 다르다.', 'fragment는 일반 HTTP 요청에 포함되지 않는다.', 'URL 관찰은 요청 허가와 별개이며 고정된 교육 대상에서만 수행한다.'],
  }),
  'w3-encoding-body': blockSet({
    id: 'w3-encoding-body', question: '같은 “과제 제출” 기능도 JSON, form-urlencoded, multipart 요청에서 왜 다른 본문 구조와 서버 검증을 가질까?',
    explanation: ['HTTP body는 method와 header, 특히 `Content-Type`에 따라 다른 형식으로 해석됩니다. JSON은 구조화된 객체·배열을, `application/x-www-form-urlencoded`는 키-값 쌍을, `multipart/form-data`는 경계(boundary)로 나뉜 여러 파트를 전달하는 데 사용합니다.', 'URL encoding, HTML encoding, Base64 같은 표현 변환은 각기 목적이 다르며 입력 검증이나 권한 검사를 대신하지 않습니다. 서버는 body 형식·크기·필드·파일 형식·권한을 신뢰 경계에서 다시 확인해야 합니다.'],
    transcript: { type: 'http-message', title: 'JSON과 multipart의 본문 구조', message: 'POST /api/assignments HTTP/1.1\nContent-Type: application/json\n\n{"title":"week-3","draft":true}\n\nPOST /api/uploads HTTP/1.1\nContent-Type: multipart/form-data; boundary=----training\n\n------training\nContent-Disposition: form-data; name="file"; filename="report.txt"\nContent-Type: text/plain\n\ntraining content\n------training--', annotations: ['JSON은 body 전체가 구조화된 값입니다.', 'multipart는 각 파트에 이름·파일명·content type 같은 메타데이터가 있을 수 있습니다.', '클라이언트가 보낸 filename·content type은 서버가 그대로 신뢰할 근거가 아닙니다.'] },
    comparison: { title: '자주 보는 body 형식', columns: ['형식', '주요 용도', '서버 확인'], rows: [['JSON', 'API의 구조화 데이터', 'schema·타입·필수 필드·권한'], ['form-urlencoded', '간단한 폼 전송', '디코딩 뒤 키·값·중복 파라미터'], ['multipart/form-data', '파일과 여러 필드', '크기·형식·저장 경로·소유자·악성 파일 정책'], ['없음', 'GET·상태 조회 등', 'query·headers·세션·인가']] },
    caseBody: 'CodeCureLAB 과제 제목은 JSON body로, 제출 파일은 multipart 파트로 올 수 있습니다. 서버는 화면에서 파일 선택을 제한했더라도 요청 본문의 실제 길이·형식·저장 대상·작성자 권한을 확인해야 합니다.', facts: ['Content-Type은 해석 방법을 알려 주지만 데이터의 진실성 보증이 아니다.', '파일 업로드는 파일명·MIME type·바이트·저장 위치·인가를 함께 검토한다.', '인코딩은 문맥에 맞는 표현 처리이며 보안 통제를 대체하지 않는다.'],
    misconception: ['Content-Type이 image/png면 이미지 파일이다 → 클라이언트 header와 파일명은 조작 가능하므로 서버 측 형식·처리 정책이 필요하다.', 'Base64면 파일 업로드 검증이 필요 없다 → 표현만 바뀌었을 뿐 크기·형식·권한 검증은 여전히 필요하다.'],
    checks: [{ title: '본문 형식', prompt: '파일과 텍스트 필드를 한 요청에 나눠 담는 데 자주 쓰는 형식은?', options: ['multipart/form-data', 'DNS TXT', 'HTTP status line'], answer: 0, explanation: 'multipart/form-data는 boundary로 나뉜 여러 파트를 전송할 수 있습니다.' }, { title: '서버 검증', prompt: '과제 업로드에서 서버가 body 형식 외에 확인할 항목을 다섯 가지 이상 적어 보세요.' }],
    work: 'API 보안과 제품 보안에서는 Content-Type과 body 파서를 신뢰 경계로 봅니다. 형식별 schema 검증, 크기 제한, 저장소 권한, 객체 인가를 함께 설계하고 테스트합니다.', labs: ['w3-http-message', 'w3-tool-triangle'], sourceItems: [sources.http, sources.messaging], summary: ['HTTP body 형식은 Content-Type과 파서에 따라 달라진다.', 'JSON, form-urlencoded, multipart는 구조와 검증 포인트가 다르다.', '클라이언트가 보낸 형식·파일명·MIME type은 서버가 다시 검증해야 한다.'],
  }),
  'w3-http': blockSet({
    id: 'w3-http', question: 'HTTP 요청과 응답에서 첫 줄, headers, 빈 줄, body를 구분하면 어떤 보안 질문을 더 정확히 할 수 있을까?',
    explanation: ['HTTP 메시지는 시작 줄, header fields, 빈 줄, 선택적인 body로 구성됩니다. 요청 시작 줄에는 method와 request target이, 응답 시작 줄에는 상태 코드가 있습니다. Headers는 메시지·표현·인증·캐시 등 메타데이터를 전달하며 body는 실제 데이터일 수 있습니다.', 'Method와 status는 이름만으로 보안 판단을 끝내지 않습니다. GET이라도 민감한 데이터를 반환할 수 있고, POST라도 서버의 인증·인가·입력 검증이 빠질 수 있습니다. 상태 코드는 서버가 표현한 처리 결과이므로 요청과 응답 전체 맥락에서 읽습니다.'],
    transcript: { type: 'http-message', title: '요청과 응답의 구조', message: 'GET /api/notices?page=1 HTTP/1.1\nHost: training.local\nAccept: application/json\n\n\nHTTP/1.1 200 OK\nContent-Type: application/json\nCache-Control: no-store\n\n{"items":[{"id":42,"title":"training notice"}]}', annotations: ['요청선과 상태선은 각각 시작 줄입니다.', '빈 줄이 headers와 body를 나눕니다.', '응답 body에 어떤 데이터가 들어가는지는 인증·인가·데이터 최소화와 함께 검토합니다.'] },
    comparison: { title: 'HTTP 구성 요소와 질문', columns: ['부분', '예시', '보안 관찰 질문'], rows: [['Method·target', '`GET /api/notices`', '어떤 동작·자원인가? 입력은 어디에 있는가?'], ['Request headers', '`Cookie`, `Accept`', '인증 상태·콘텐츠 협상이 어떻게 전달되는가?'], ['Request body', 'JSON·form data', '서버가 형식·필드·권한을 재검증하는가?'], ['Status·response headers', '`200`, `Cache-Control`', '처리 결과·캐시·보안 속성을 어떻게 표현하는가?'], ['Response body', 'JSON·HTML', '누구에게 어떤 데이터가 반환되는가?']] },
    caseBody: 'CodeCureLAB 공지 API가 200과 목록 JSON을 돌려줘도, 그 목록이 로그인 전 공개인지 학생별 접근 제어가 필요한지, 캐시가 허용되는지, 오류 시 어떤 정보가 노출되는지를 별도로 확인해야 합니다.', facts: ['HTTP 메시지 해부는 취약점 결론이 아니라 관찰의 공통 언어다.', 'method·status·header·body는 서로 다른 질문을 제공한다.', '정상 응답과 안전한 인가·데이터 처리는 같은 뜻이 아니다.'],
    misconception: ['POST 요청이면 자동으로 안전하다 → method는 동작 의도를 나타낼 뿐 인증·인가·검증을 구현하지 않는다.', '403이면 취약점이 없다 → 한 요청의 상태 코드만으로 다른 객체·역할·경로의 검증을 단정할 수 없다.'],
    checks: [{ title: '메시지 경계', prompt: 'HTTP headers와 body를 나누는 표시는?', options: ['빈 줄', 'DNS 응답', '파일 권한'], answer: 0, explanation: 'HTTP/1.1 메시지 형식에서 빈 줄은 header section의 끝을 나타냅니다.' }, { title: '응답 읽기', prompt: '200 JSON 응답을 받았을 때 데이터 노출과 인가 관점에서 이어서 확인할 질문을 적어 보세요.' }],
    work: '취약점 보고서와 API 리뷰는 HTTP 원문을 근거로 요청·응답·상태·필드를 재현 가능하게 기록합니다. 민감한 값은 마스킹하고, 관찰과 영향 판단을 분리합니다.', labs: ['w3-http-message', 'w3-tool-triangle'], sourceItems: [sources.http, sources.messaging], summary: ['HTTP는 시작 줄·headers·빈 줄·선택 body로 읽는다.', 'method와 status만으로 보안 검증 여부를 판단하지 않는다.', 'HTTP 원문은 관찰 근거이며 인증·인가·데이터 판단은 별도 질문이다.'],
  }),
  'w3-headers': blockSet({
    id: 'w3-headers', question: 'Cookie, Cache-Control, Content-Security-Policy 같은 header는 어떤 위험을 줄일 수 있고 무엇을 대신할 수 없을까?',
    explanation: ['HTTP headers는 콘텐츠 형식, 캐시, 인증 상태, 브라우저 정책 등 메시지의 처리를 지시하거나 설명합니다. `Content-Type`, `Cache-Control`, `Set-Cookie`, `Location`, 보안 관련 정책 헤더는 자주 보지만, 이름만 보고 적용 효과를 과장하지 않습니다.', '예를 들어 `HttpOnly`는 JavaScript의 Cookie 읽기를 제한하는 속성이고, `Secure`는 HTTPS 전송을 제한하는 속성이며, `SameSite`는 교차 사이트 Cookie 전송 정책에 관여합니다. 이들은 서버의 객체 인가나 입력 검증을 대체하지 않습니다. CSP도 근본 원인 수정과 함께 쓰는 심층 방어입니다.'],
    transcript: { type: 'http-message', title: '응답 header를 하나씩 읽기', message: 'HTTP/1.1 302 Found\nLocation: /dashboard\nSet-Cookie: cc_session=[REDACTED]; Path=/; HttpOnly; Secure; SameSite=Lax\nCache-Control: no-store\nContent-Security-Policy: default-src \'self\'\n\n', annotations: ['302와 Location은 다음 위치를 가리키는 응답입니다.', 'Cookie 속성은 상태 전송 조건에 관여합니다.', '`no-store`와 CSP는 각각 캐시·브라우저 실행 정책에 관여하지만 서버 인가를 대신하지 않습니다.'] },
    comparison: { title: '대표 header·속성의 역할', columns: ['항목', '도움이 되는 것', '대신하지 않는 것'], rows: [['`Content-Type`', '본문 해석 방식 전달', '입력·파일 형식의 서버 검증'], ['`Cache-Control: no-store`', '일부 저장 재사용 제한', '응답 데이터의 인가·최소화'], ['`HttpOnly`', 'JS의 Cookie 읽기 제한', 'XSS 실행·사용자 동작 대행 방지'], ['`SameSite`', '교차 사이트 Cookie 전송 정책', '서버의 CSRF 토큰·인가 검증 전체'], ['CSP', '허용 리소스·실행 경로 제한', '취약한 출력 처리의 근본 수정']] },
    caseBody: 'CodeCureLAB 로그인 응답은 세션 Cookie에 속성을 붙이고 개인 프로필 응답에는 캐시 정책을 둘 수 있습니다. 하지만 `/api/profile?id=...`가 요청자와 대상의 관계를 서버에서 확인하지 않으면 header만으로 다른 사용자 데이터 접근을 막을 수 없습니다.', facts: ['headers와 Cookie 속성은 defense-in-depth의 한 층이다.', '정책의 적용 범위·브라우저 지원·서버 구성에 따라 효과가 달라질 수 있다.', '서버 인증·인가·입력 검증은 별도 통제다.'],
    misconception: ['HttpOnly가 있으면 XSS 영향이 없다 → Cookie 읽기를 제한할 뿐 같은 Origin의 DOM·요청·사용자 동작 위험을 모두 없애지 않는다.', 'CSP가 있으니 출력 인코딩을 하지 않아도 된다 → CSP는 보조 통제이며 안전한 출력 처리의 대체물이 아니다.'],
    checks: [{ title: '속성의 한계', prompt: 'JavaScript에서 Cookie 값을 읽는 것을 제한하는 속성은?', options: ['HttpOnly', 'Content-Length', 'ETag'], answer: 0, explanation: 'HttpOnly는 document.cookie 등 JavaScript 접근을 제한합니다.' }, { title: '심층 방어', prompt: '프로필 API에 Cache-Control과 Cookie 속성을 추가해도 서버에서 반드시 유지해야 하는 검증을 적어 보세요.' }],
    work: '보안 엔지니어와 웹 개발자는 header를 복사해 붙이는 대신 자산·경로·위협·브라우저·서버 처리 범위를 함께 설계합니다. 보고서에는 header 존재 여부와 실제 한계를 모두 기록합니다.', labs: ['w3-cookie', 'w3-tool-triangle'], sourceItems: [sources.http, sources.cookies, sources.html], summary: ['headers와 Cookie 속성은 메시지·브라우저 처리에 관여한다.', 'HttpOnly·SameSite·CSP는 각기 제한된 범위의 심층 방어다.', '서버 인증·인가·입력 검증을 대체하지 않는다.'],
  }),
  'w3-session': blockSet({
    id: 'w3-session', question: 'Cookie에 세션 ID가 있다는 사실과 서버에 로그인 상태가 있다는 사실은 왜 같은 말이 아닐까?',
    explanation: ['세션 기반 인증에서 서버는 로그인 성공 뒤 세션 상태를 만들고, 브라우저는 서버가 찾을 수 있는 세션 식별자를 Cookie 등에 보관해 다음 요청에 보냅니다. Cookie 값은 서버 세션 데이터 자체가 아니라 서버가 상태를 조회하는 데 쓰는 식별자일 수 있습니다.', '식별, 인증, 인가를 구분해야 합니다. 사용자가 세션 식별자를 보냈다는 것은 요청 주체를 찾기 위한 단서이고, 서버는 세션의 유효성·만료·폐기 상태를 확인한 뒤 각 자원에 대한 인가를 다시 판단합니다. 로그아웃·비밀번호 변경·권한 변경 등에서 세션 수명주기 관리가 중요합니다.'],
    timeline: { title: '세션 수명주기', items: [{ title: '로그인 요청', body: '자격 증명 검증과 필요한 보안 정책을 수행한다.' }, { title: '세션 생성', body: '서버가 상태와 만료·회전 정책을 만든다.' }, { title: 'Set-Cookie 응답', body: '브라우저가 다음 요청에 보낼 식별자와 속성을 받는다.' }, { title: '후속 요청', body: '서버가 세션 유효성과 객체 인가를 확인한다.' }, { title: '로그아웃·만료·폐기', body: '서버 상태와 브라우저 저장소를 함께 정리하고 재사용을 막는다.' }] },
    transcript: { type: 'http-message', title: '세션 ID와 서버 상태를 분리해 보기', message: 'POST /login HTTP/1.1\n\nHTTP/1.1 302 Found\nSet-Cookie: cc_session=demo-session-redacted; HttpOnly; Secure; SameSite=Lax\n\nGET /api/profile HTTP/1.1\nCookie: cc_session=demo-session-redacted\n\nServer check: session valid? user=student-102? profile object authorized?', annotations: ['Cookie는 브라우저가 보내는 식별자입니다.', '서버는 식별자만 보고 신뢰하지 않고 유효성·만료·폐기 상태를 확인합니다.', '로그인 성공 뒤에도 요청 대상에 대한 인가는 별도입니다.'] },
    comparison: { title: '상태 개념 구분', columns: ['개념', '무엇인가', '서버가 확인할 것'], rows: [['Cookie', '브라우저가 저장·전송하는 name=value와 속성', '전송 조건·무결성·민감 값 노출'], ['세션 ID', '서버 상태를 찾는 식별자', '예측 불가성·회전·만료·폐기'], ['세션 상태', '서버가 가진 로그인·역할·수명 정보', '유효성·탈취 의심·권한 변경'], ['인가', '특정 자원·행동 허용 판단', '요청 주체와 대상 객체의 관계']] },
    caseBody: 'CodeCureLAB 학생이 로그인 후 자신의 프로필을 열 때 브라우저는 Cookie를 보내고 서버는 세션을 찾아 학생 ID를 확인한 뒤 요청한 프로필 객체가 그 학생에게 허용되는지 판단합니다. Cookie가 있다는 것만으로 다른 학생 객체 접근이 허용되면 안 됩니다.', facts: ['세션 ID는 비밀번호나 프로필 데이터가 아니다.', '세션 유효성·만료·폐기와 객체 인가는 서로 다른 검증이다.', '세션 식별자는 로그·캡처·보고서에서 마스킹한다.'],
    misconception: ['Cookie가 곧 서버 세션 데이터다 → Cookie는 상태를 가리키는 식별자일 수 있고 서버 상태와 분리된다.', '로그인했으면 모든 프로필 URL에 접근 가능하다 → 자원별 인가는 후속 요청마다 확인해야 한다.'],
    checks: [{ title: '개념 구분', prompt: '서버가 로그인 상태를 찾기 위해 Cookie에서 받는 값에 가장 가까운 것은?', options: ['세션 식별자', '전체 사용자 DB', 'DOM 노드'], answer: 0, explanation: '세션 식별자는 서버가 세션 상태를 조회하는 키로 쓰일 수 있습니다.' }, { title: '세션 검증', prompt: '로그아웃·권한 변경 후 세션과 객체 인가 관점에서 서버가 확인·갱신할 항목을 적어 보세요.' }],
    work: '웹 개발, IAM, 사고 대응은 세션 생성·회전·만료·로그아웃·권한 변경을 설계와 로그에서 추적합니다. 세션 문제는 Cookie 하나의 속성보다 서버 상태와 인가 흐름을 함께 봐야 합니다.', labs: ['w3-cookie', 'w3-tool-triangle'], sourceItems: [sources.cookies, sources.http], summary: ['Cookie, 세션 식별자, 서버 세션 상태, 객체 인가는 다른 개념이다.', '후속 요청마다 서버는 세션 유효성과 자원 인가를 확인한다.', '세션 식별자는 민감한 인증 보조 정보로 마스킹한다.'],
  }),
  'w3-auth-origin': blockSet({
    id: 'w3-auth-origin', question: '같은 사용자가 로그인한 상태여도 왜 브라우저 Origin과 서버의 객체 인가 판단을 분리해서 이해해야 할까?',
    explanation: ['Origin은 보통 scheme, host, port의 조합입니다. Same-Origin Policy(SOP)는 한 Origin의 문서와 script가 다른 Origin의 데이터에 임의로 접근하지 못하도록 제한하는 브라우저 보안 모델입니다. CORS는 서버가 어떤 다른 Origin의 브라우저 요청·응답 공유를 허용할지 header로 표현하는 메커니즘입니다.', 'SOP와 CORS는 브라우저 정책이고, 서버의 인증·객체 인가를 대체하지 않습니다. 서버는 요청자가 어떤 세션·토큰을 통해 인증됐는지, 요청한 객체에 권한이 있는지, 상태 변경 요청에 필요한 검증이 있는지를 자체적으로 판단해야 합니다.'],
    transcript: { type: 'http-message', title: 'Origin과 서버 인가의 질문', message: 'Page origin: https://training.local\nRequest target: https://api.training.local/profile\nOrigin header: https://training.local\n\nServer questions:\n1. Is the requesting session authenticated?\n2. Is student-102 allowed to read this profile object?\n3. Does CORS policy allow browser response sharing?', annotations: ['Origin 비교는 브라우저가 응답을 script에 공유하는 조건과 관련됩니다.', '서버 객체 인가는 같은 Origin 요청에도 필요합니다.', 'CORS 허용 header가 있다고 해서 모든 데이터 접근을 허용하는 것은 아닙니다.'] },
    comparison: { title: '브라우저 정책과 서버 통제', columns: ['항목', '주요 질문', '대체하지 않는 것'], rows: [['SOP', '다른 Origin 문서가 응답에 접근할 수 있는가?', '서버 인증·객체 인가'], ['CORS', '서버가 어떤 Origin에 응답 공유를 허용하는가?', 'URL별·사용자별 권한 판단'], ['인증', '요청 주체를 확인했는가?', '특정 객체·행동 허용'], ['객체 인가', '이 주체가 이 데이터·행동을 해도 되는가?', '브라우저 Origin 정책']] },
    caseBody: 'CodeCureLAB 웹 UI와 API가 다른 subdomain에 있어 CORS 정책이 필요할 수 있습니다. 하지만 API가 `/profiles/103` 요청을 받을 때 세션의 학생 102가 103의 프로필을 볼 수 있는지 확인하는 것은 Origin 비교가 아니라 서버 객체 인가 문제입니다.', facts: ['Origin은 scheme·host·port의 조합으로 본다.', 'CORS는 브라우저 응답 공유 정책이다.', '서버는 요청 주체·대상 객체·행동을 독립적으로 검증한다.'],
    misconception: ['CORS 오류가 없으면 API 인가가 안전하다 → CORS와 객체 인가는 다른 계층의 통제다.', '같은 Origin 요청이면 서버 권한 검사가 필요 없다 → 같은 Origin에서도 다른 사용자·역할·객체에 대한 인가가 필요하다.'],
    checks: [{ title: '책임 구분', prompt: '학생 102가 학생 103의 프로필을 볼 수 있는지 판단하는 주체는?', options: ['서버의 객체 인가 로직', '브라우저 글꼴', 'DNS 캐시'], answer: 0, explanation: '요청 주체와 대상 객체 관계는 서버가 판단해야 합니다.' }, { title: '정책 분리', prompt: 'SOP·CORS·인증·객체 인가가 각각 답하는 질문을 CodeCureLAB API 예로 한 줄씩 적어 보세요.' }],
    work: 'API 보안 리뷰에서는 browser policy 오류와 서버 인가 오류를 분리합니다. 서로 다른 통제를 한 개의 “접근 제한”으로 묶으면 실제 책임 위치와 수정 방법이 흐려집니다.', labs: ['w3-cookie', 'w3-threat-model'], sourceItems: [sources.fetch, sources.http], summary: ['Origin은 scheme·host·port 조합이며 SOP는 브라우저 정책이다.', 'CORS는 응답 공유의 서버 선언이고 객체 인가가 아니다.', '서버는 인증 후에도 대상 객체와 행동을 별도로 인가한다.'],
  }),
  'w3-browser-runtime': blockSet({
    id: 'w3-browser-runtime', question: '서버가 보낸 HTML 원문, JavaScript가 가진 데이터, 화면의 DOM은 왜 서로 다른 증거일 수 있을까?',
    explanation: ['브라우저는 HTML을 파싱해 DOM을 만들고 CSS를 적용하며 JavaScript를 실행합니다. JavaScript는 Fetch 응답, URL fragment, form input, localStorage 같은 여러 Source에서 데이터를 읽고 DOM API로 화면을 바꿀 수 있습니다.', '따라서 서버 response view에 입력이 없다고 해서 브라우저에 영향이 없다는 뜻은 아닙니다. DevTools의 Network는 원문 요청·응답을, Sources는 script를, Elements는 실행 후 DOM을 보여 줍니다. Week 3에서는 이 차이를 Source·Transform·Sink·Context로 더 깊게 추적합니다.'],
    transcript: { type: 'code', title: '응답 데이터와 DOM 갱신', language: 'javascript', code: "const response = await fetch('/api/notices');\nconst data = await response.json();\ncount.textContent = String(data.items.length);\n\n// 같은 화면이라도 response JSON, JS 변수, DOM textContent는 다른 관찰 지점이다.", annotations: ['Fetch 응답은 JavaScript가 읽는 데이터 Source가 될 수 있습니다.', '`textContent`는 문자열을 텍스트로 넣는 DOM API입니다.', '실제 동작과 보안성은 데이터 출처·변환·최종 Sink 문맥을 함께 봐야 합니다.'] },
    comparison: { title: '브라우저에서 보는 세 층', columns: ['관찰 지점', '무엇을 보는가', '대표 도구'], rows: [['Server response', '서버가 반환한 HTML·JSON 원문', 'DevTools Network'], ['JavaScript runtime', '변수·함수·Fetch·이벤트 처리', 'DevTools Sources·Console'], ['Executed DOM', '파싱과 JS 실행 뒤 문서 구조', 'DevTools Elements']] },
    caseBody: 'CodeCureLAB 공지 목록은 서버가 빈 HTML shell을 보내고 JavaScript가 `/api/notices` JSON을 읽어 DOM을 채울 수 있습니다. 이때 화면의 제목은 response HTML에 없고 실행 후 Elements에서만 보일 수 있습니다.', facts: ['서버 원문, JS 데이터, 실행 DOM은 서로 다른 증거다.', 'DevTools 패널은 같은 페이지의 다른 층을 보여 준다.', '보안 검토에서는 데이터 Source와 최종 DOM API·문맥을 연결한다.'],
    misconception: ['Elements에서 보이는 DOM이 서버가 그대로 보낸 HTML이다 → 브라우저 파싱과 JavaScript 실행 뒤의 결과일 수 있다.', 'JavaScript가 데이터를 다루면 서버 검증은 필요 없다 → 클라이언트 코드는 신뢰 경계가 아니며 서버가 권한·입력을 재검증한다.'],
    checks: [{ title: '관찰 위치', prompt: 'JavaScript 실행 뒤 실제 문서 구조를 보는 DevTools 패널은?', options: ['Elements', 'Network DNS cache', 'Git log'], answer: 0, explanation: 'Elements는 실행 후 DOM 구조를 관찰하는 데 사용합니다.' }, { title: '증거 비교', prompt: '화면에 표시된 값이 Network response에 없을 때 Sources·Elements에서 각각 무엇을 확인할지 적어 보세요.' }],
    work: '프론트엔드 보안과 XSS 분석은 response, runtime data, DOM을 구분합니다. 어떤 데이터가 어디서 왔고 어떤 API로 어떤 문맥에 들어갔는지 기록해야 취약점 원인과 수정 방향을 설명할 수 있습니다.', labs: ['w3-source-sink', 'w3-tool-triangle'], sourceItems: [sources.html, sources.fetch, sources.mdn], summary: ['브라우저는 HTML 파싱과 JavaScript 실행으로 DOM을 만든다.', '서버 response·JS runtime·executed DOM은 다른 관찰 지점이다.', '클라이언트 처리는 서버 인증·인가·입력 검증을 대신하지 않는다.'],
  }),
  'w3-dom': blockSet({
    id: 'w3-dom', question: '신뢰할 수 없는 데이터가 DOM에 도달할 때 왜 `textContent`와 HTML을 해석하는 API의 차이를 알아야 할까?',
    explanation: ['DOM은 브라우저가 문서를 객체 구조로 표현한 모델입니다. JavaScript는 `textContent`, `createTextNode`, 속성 설정, HTML 파싱 API 등으로 DOM을 바꿀 수 있습니다. 데이터가 어떤 Source에서 왔는지, 어떤 변환을 거쳤는지, 어떤 Sink와 문맥에 도달하는지가 보안 검토의 핵심입니다.', '이 주차에서는 위험한 payload를 만들지 않고 Source·Transform·Sink·Context의 위치를 읽습니다. Week 3에서 XSS의 배경과 방어를 깊게 다루며, 안전한 기본값은 신뢰할 수 없는 데이터를 텍스트로 처리하고 컨텍스트별 출력 인코딩과 검증된 방식을 사용하는 것입니다.'],
    transcript: { type: 'code', title: '텍스트 삽입과 HTML 해석 API 비교', language: 'javascript', code: "const title = new URLSearchParams(location.search).get('title') || '';\n\n// 텍스트로 표시\nheading.textContent = title;\n\n// HTML을 해석하는 API는 입력 출처·문맥·방어를 별도로 검토해야 한다.\n// preview.innerHTML = trustedTemplate;", annotations: ['`location.search`는 브라우저 측 Source의 한 예입니다.', '`textContent`는 값을 텍스트 노드로 다룹니다.', 'HTML을 해석하는 API는 신뢰할 수 없는 데이터를 넣지 않고, 필요한 경우 검증된 정책과 컨텍스트별 방어를 사용합니다.'] },
    comparison: { title: '데이터 흐름을 읽는 네 요소', columns: ['요소', '질문', 'CodeCureLAB 예'], rows: [['Source', '데이터는 어디서 왔는가?', 'query, fragment, form, API response, localStorage'], ['Transform', '어떻게 변환·검증·조합됐는가?', 'decode, template render, string formatting'], ['Sink', '최종적으로 어떤 API에 전달되는가?', 'textContent, attribute setter, HTML parsing API'], ['Context', '브라우저가 어떤 문법으로 해석하는가?', 'HTML text, attribute, URL, JavaScript, CSS']] },
    caseBody: 'CodeCureLAB 검색어는 URL query에서 읽혀 화면 제목에 표시될 수 있습니다. 서버가 반사한 값인지, JavaScript가 읽은 값인지, 텍스트로 표시했는지 HTML로 해석했는지는 서로 다른 사실입니다. 이를 한 줄의 “XSS” 결론으로 묶지 않습니다.', facts: ['Source와 Sink 사이에는 여러 변환과 신뢰 경계가 있을 수 있다.', '같은 문자열도 HTML·attribute·URL·JS 등 문맥에 따라 방어가 달라진다.', 'Week 3 실습은 고정된 무해한 사례에서 이 흐름을 보고서 형식으로 기록한다.'],
    misconception: ['입력을 한 번 필터링하면 모든 문맥에서 안전하다 → 파서와 문맥이 달라 출력 위치별 방어가 필요하다.', 'innerHTML을 쓰지 않으면 모든 DOM 문제가 사라진다 → 데이터 출처·속성·URL·서버 인가 등 다른 경계도 함께 검토한다.'],
    checks: [{ title: '요소 구분', prompt: 'URL query에서 읽은 값을 `heading.textContent`에 넣는 흐름에서 Sink는?', options: ['`heading.textContent`', 'URL query 자체', 'DNS 응답'], answer: 0, explanation: 'Sink는 데이터가 최종적으로 전달되어 화면·동작에 영향을 주는 API나 위치입니다.' }, { title: 'Week 3 준비', prompt: '검색 기능 하나를 골라 Source, Transform, Sink, Context를 각각 무엇으로 기록할지 적어 보세요.' }],
    warning: { title: 'Week 3으로의 연결', body: '이 모듈에서는 XSS payload·우회 문자열을 만들거나 실행하지 않습니다. 다음 주차의 고정된 격리 사례에서 무해한 마커, 근본 원인, 방어 코드, 재시험을 다룹니다.' }, work: 'AppSec과 프론트엔드 개발은 데이터 흐름과 DOM API를 설계·리뷰합니다. 보고서에는 Source만 적지 말고 변환·Sink·문맥·영향 가설·수정·재시험까지 연결합니다.', labs: ['w3-source-sink', 'w3-threat-model'], sourceItems: [sources.html, sources.fetch], summary: ['DOM 보안 검토는 Source·Transform·Sink·Context를 연결한다.', '같은 문자열도 최종 문맥에 따라 안전한 처리 방식이 달라진다.', 'Week 3에서는 고정된 격리 사례에서 XSS 배경·방어·보고서를 이어간다.'],
  }),
})
